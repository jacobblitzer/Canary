{
  "name": "cpig-booleans-02-difference-sphere-sphere",
  "workload": "rhino",
  "description": "Phase D4 boolean audit (B0) â€” SIGN CONVENTION CHECK. Difference of two CPig tape spheres. The CPig convention (research dive Â§1.2: field_binary_impl 'difference' is max(fa->tree, -(fb->tree))) keeps the FIRST-picked operand and subtracts the second. So _SelAll picks A then B (whichever order Rhino delivers); _CPigDifference keeps A, subtracts B. With A=sphere(0,0,0;r=10) and B=sphere(15,0,0;r=10), expect: a SINGLE sphere centered near origin with a CRESCENT BITE taken out of its +x side (B subtracted). If we see a sphere at +x with a bite from its -x side, the operand order was reversed â€” that's the surface to watch (research dive B3: prompt doesn't clarify which is minuend).",
  "runMode": "shared",
  "setup": {
    "viewport": { "projection": "Perspective", "displayMode": "Shaded", "width": 800, "height": 600 },
    "vlm": { "provider": "ollama", "model": "qwen2.5vl:7b" },
    "vlmDescription": "A 3D ray-marched signed-distance surface showing one sphere with a curved bite taken out of one side (a crescent moon shape, or a sphere minus a sphere). NOT a single intact sphere, NOT a peanut, NOT two separate spheres."
  },
  "actions": [
    { "type": "RunCommand", "command": "_SelAll _Delete" },
    { "type": "RunCommand", "command": "_CPigSphere 0,0,0 10" },
    { "type": "WaitForPenumbraFrame", "requireReal": true, "timeoutMs": 110000 },
    { "type": "RunCommand", "command": "_CPigSphere 15,0,0 10" },
    { "type": "WaitForPenumbraFrame", "requireReal": true, "timeoutMs": 60000 },
    { "type": "RunCommand", "command": "_SelAll _CPigDifference" },
    { "type": "WaitForPenumbraFrame", "requireReal": true, "timeoutMs": 60000 },
    { "type": "RunCommand", "command": "_SelNone" },
    { "type": "RunCommand", "command": "-_4View _Enter" },
    { "type": "RunCommand", "command": "-_MaxViewport" },
    { "type": "RunCommand", "command": "-_SetView _World _Perspective" },
    { "type": "RunCommand", "command": "-_Sphere 0,0,0 45" },
    { "type": "RunCommand", "command": "_SelNone" },
    { "type": "RunCommand", "command": "_SelLast" },
    { "type": "RunCommand", "command": "-_Zoom _Selected" },
    { "type": "RunCommand", "command": "-_ViewportProperties _Lens 35 _EnterEnd" },
    { "type": "RunCommand", "command": "-_ViewportProperties _Lens 50 _EnterEnd" },
    { "type": "RunCommand", "command": "_Delete" },
    { "type": "WaitForPenumbraFrame", "requireReal": true, "requireSteady": true, "timeoutMs": 90000 }
  ],
  "checkpoints": [
    {
      "name": "cpig-booleans-difference-sphere-sphere",
      "atTimeMs": 0,
      "source": "viewport",
      "mode": "capture",
      "stabilizeMs": 5000,
      "tolerance": 0.05,
      "description": "Difference(sphere_A, sphere_B) â€” one sphere with a curved bite taken out of one side. Operator should verify WHICH side has the bite to confirm sign convention."
    }
  ]
}
