{
  "name": "bristle",
  "description": "Bristle GH client (BR_ components) against a LIVE local engine: connect/health, paint_v0 submit, background-poll to done, StrokeSet fetch + export. PREREQ per BRISTLE_WORKLOAD.md: the engine service must be running on this machine before the suite starts.",
  "tests": [
    "bristle-01-paint-roundtrip"
  ]
}
