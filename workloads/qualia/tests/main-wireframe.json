{
  "name": "main-wireframe",
  "workload": "qualia",
  "description": "Wireframe display mode — edges + labels only, nodes are invisible. Tests that nodeOpacity=0 hides instanced spheres while keeping edges + labels.",
  "setup": {
    "canvas": { "width": 1280, "height": 800 },
    "commands": [
      "window.__canaryWaitForReady(30000)",
      "window.__canaryCloseLandingScreen()",
      "window.__canaryApplyProfile('wireframe')"
    ]
  },
  "checkpoints": [
    { "name": "main-wireframe", "stabilizeMs": 2500, "tolerance": 0.3 }
  ]
}
