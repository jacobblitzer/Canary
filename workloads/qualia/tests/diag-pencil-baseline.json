{
  "name": "diag-pencil-baseline",
  "workload": "qualia",
  "description": "Pencil profile baseline INSIDE the Papers qverse (which has sphere atoms — Atlas only has signposts). Diagnostic overlay shows persona enable state + renderer toneMapping + scene background type + edge colors after FX overrides.",
  "setup": {
    "canvas": { "width": 1280, "height": 800 },
    "commands": [
      "window.__canaryWaitForReady(30000)",
      "window.__canaryCloseLandingScreen()",
      "window.__canarySetTheme('dark')",
      "window.__canaryApplyProfile('pencil')",
      "window.__canarySwitchContext('ctx-papers')",
      "window.__canaryResetAndRepel(1200)",
      "window.__canaryDumpDiagnostics()"
    ]
  },
  "checkpoints": [
    { "name": "diag-pencil-baseline", "stabilizeMs": 3500, "tolerance": 0.3 }
  ]
}
