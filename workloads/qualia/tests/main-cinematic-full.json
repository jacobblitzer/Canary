{
  "name": "main-cinematic-full",
  "workload": "qualia",
  "description": "Cinematic profile end-to-end — bloom + curl-noise particles + Penumbra backdrop all active. Used as a visual oracle to confirm every display module renders together without interop bugs. Companion to main-cinematic-curl-noise (which disables Penumbra to isolate particle/bloom rendering).",
  "setup": {
    "canvas": { "width": 1280, "height": 800 },
    "commands": [
      "window.__canaryWaitForReady(30000)",
      "window.__canaryCloseLandingScreen()",
      "window.__canaryApplyProfile('cinematic')"
    ]
  },
  "checkpoints": [
    {
      "name": "main-cinematic-full",
      "stabilizeMs": 3000,
      "tolerance": 0.3
    }
  ]
}
