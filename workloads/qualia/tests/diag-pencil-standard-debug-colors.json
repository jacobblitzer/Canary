{
  "name": "diag-pencil-standard-debug-colors",
  "workload": "qualia",
  "description": "[STALE 2026-08-18] This test calls __canarySetPersonaEnabled with module id(s) fx.debug-layer-colors, which no longer exist in Qualia's module registry (packages/core/src). The call is a SILENT NO-OP: the test still runs and still captures, but the toggle it is named for does nothing, so the frame is indistinguishable from the un-toggled one. Repair by mapping to a live module id, or retire the arm. Standard profile + fx.debug-layer-colors — inside Papers qverse. Baseline for the debug palette without pencil-specific state.",
  "setup": {
    "canvas": {
      "width": 1280,
      "height": 800
    },
    "commands": [
      "window.__canaryWaitForReady(30000)",
      "window.__canaryCloseLandingScreen()",
      "window.__canarySetTheme('dark')",
      "window.__canaryApplyProfile('standard')",
      "window.__canarySetPersonaEnabled('fx.debug-layer-colors', true)",
      "window.__canarySwitchContext('ctx-papers')",
      "window.__canaryResetAndRepel(1200)",
      "window.__canaryDumpDiagnostics()"
    ]
  },
  "checkpoints": [
    {
      "name": "diag-pencil-standard-debug-colors",
      "stabilizeMs": 3500,
      "tolerance": 0.3,
      "mode": "capture"
    }
  ]
}
